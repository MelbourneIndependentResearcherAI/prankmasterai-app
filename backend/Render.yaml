services:
  - type: web
    name: prankmasterai-backend
    env: node
    region: oregon
    plan: free
    buildCommand: "npm install"
    startCommand: "node server.js"
    rootDir: backend
    autoDeploy: true
    envVars:
      - key: NODE_VERSION
        value: 18
